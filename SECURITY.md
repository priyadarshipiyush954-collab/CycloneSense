# Security
Never commit API keys, cloud credentials, private datasets or deployment tokens.
For production, restrict CORS, add rate limiting/authentication, validate uploads,
scan images/containers and store secrets only in GitHub/hosting secret stores.
